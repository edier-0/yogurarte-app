services:
  postgres:
    image: postgres:16-alpine
    container_name: yogurarte_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
      POSTGRES_DB: yogurarte_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d yogurarte_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yogurarte_app
    restart: always
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      NODE_ENV: production
      DATABASE_URL: "postgresql://postgres:postgrespassword@postgres:5432/yogurarte_db?schema=public"
      JWT_SECRET: "${JWT_SECRET:-ba6822f65365a699a960575fe416b3a00770fd1659c34654d56e8c955ce6a78a}"
      JWT_EXPIRES_IN: "${JWT_EXPIRES_IN:-1d}"
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
    driver: local
